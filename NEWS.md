# rsyncrosim 2.1.6

## Breaking changes:

## Bug fixes:

* Fix bug in `chartData()` function that failed internally when trying to call `saveDatasheet()` and it could not find the `ChartId` column

## New features:

## Minor improvements and fixes:


